apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "seafile-server.fullname" . }}-test-connection"
  labels:
{{ include "seafile-server.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "seafile-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
